export const staffSelector = (state) => state.staff.item;
export const staffListSelector = (state) => state.staff.itemList;
export const totalElementsSelector = (state) => state.staff.totalElements;
export const loadingSelector = (state) => state.staff.loading;