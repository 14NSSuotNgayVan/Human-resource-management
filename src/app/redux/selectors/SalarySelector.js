export const getSalaries = (state)=> state.salary.salaryList;
export const getSalariesItem = (state)=> state.salary.salaryItem;
export const getShouldUpdateSalary = (state)=> state.salary.shouldUpdate;
